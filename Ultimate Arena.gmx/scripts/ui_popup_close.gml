#define ui_popup_close
with (zui_get_parent()) {
 with (black)
  zui_destroy();

 zui_destroy();
}

#define ui_resume
with (zui_get_parent()) {
 with (black)
  zui_destroy();

 zui_destroy();
}
global.GAME_SPEED = global.SAVE_SPEED;
